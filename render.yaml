services:
  - type: web
    name: cebulawfirm
    runtime: node
    buildCommand: bun install && bun run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production

