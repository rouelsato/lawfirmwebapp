services:
  - type: web
    name: cebulawfirm
    runtime: node
    buildCommand: bun install && bun run build
    startCommand: node server.mjs
    envVars:
      - key: NODE_ENV
        value: production

