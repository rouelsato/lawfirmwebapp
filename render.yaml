services:
  - type: static_site
    name: cebulawfirm
    buildCommand: bun install && bun run build
    staticPublishPath: ./dist/client
    envVars:
      - key: NODE_ENV
        value: production

